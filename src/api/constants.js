const DEFAULT_MAX_RESULTS = 10;
const DEFAULT_START_INDEX = 0;
const PAGE_NOT_FOUND = 'Sorry! page not found';
const PARAMETERS_OF_ORDER_BY = ['votes','-votes','title','-title'];

module.exports = {
    DEFAULT_MAX_RESULTS,
    DEFAULT_START_INDEX,
    PAGE_NOT_FOUND,
    PARAMETERS_OF_ORDER_BY
};
